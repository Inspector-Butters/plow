{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions for the Plow main window",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-is-focused",
    "notification:default",
    "process:allow-restart",
    "updater:default"
  ]
}
